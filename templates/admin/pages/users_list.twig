{% extends 'admin/base.html.twig' %}

{#{% block Searchform %}#}
    {#{{ form_start(form) }}#}
    {#{{ form_errors(form) }}#}

    {#{{ form_row(form.inquiry) }}#}
    {#<span class="input-group-btn">#}
        {#{{ form_row(form.submit) }}#}
    {#</span>#}
    {#{{ form_end(form) }}#}
{#{% endblock Searchform %}#}

{% block content %}
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Users</h1>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        List
                    </div>
                    <!-- /.panel-heading -->
                    <div class="panel-body">
                        <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                            <thead>
                            <tr>
                                <th>Id</th>
                                <th>Username</th>
                                <th>Email</th>
                                <th>Name</th>
                                <th>LastName</th>
                                <th>Address</th>
                                <th>Phone</th>
                                <th>Role</th>
                                <th>actions</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for user in users %}
                                <tr>
                                    <td>{{ user.id }}</td>
                                    <td>{{ user.username }}</td>
                                    <td>{{ user.email }}</td>
                                    <td>{{ user.firstName|default('empty') }}</td>
                                    <td>{{ user.lastName|default('empty')  }}</td>
                                    <td>{{ user.address|default('empty')  }}</td>
                                    <td>{{ user.phone|default('empty')  }}</td>
                                    {% for role in user.roles %}
                                    <td>{{ role }}</td>
                                    {% endfor %}
                                    {#<td>{{ user.createdAt ? user.createdAt|date('Y-m-d H:i:s') : '' }}</td>#}
                                    <td>
                                        <a href="{{ path('admin_user_edit', {'id': user.id}) }}">edit</a>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="9">users not found</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    <!-- /.panel -->
                </div>
                <!-- /.col-lg-6 -->
            </div>
            <!-- /.row -->
        </div>
        <!-- /#page-wrapper -->

    </div>
    <!-- /#wrapper -->

{% endblock content %}